  <div class="flex gap-4 pt-10">
    <div class="self-center">
      <h2 class="font-bold text-2xl pb-2">{{ holiday.name }}</h2>
      <p class="mb-4">{{ holiday.description }}</p>
    {{#if movies}}
    <h3 class="inline-block pr-4">Movies: </h3>
    {{#each movies}}
      {{!-- <a class="px-2 py-0.5 rounded-2xl bg-primary text-accent border-2 border-secondary inline-block" href="/movies/{{ this.id }}">{{ this.overview }}</a> --}}
      {{> holidb-movie-summary}}
    {{/each}}
    {{/if}}
    </div>
  </div>